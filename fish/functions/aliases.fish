function aliases -d 'alias definitions'
  alias p=prevd
  alias n=nextd
  alias e="$EDITOR"
  alias ...="cd ../../"
  alias ....="cd ../../../"
  # exa:
  alias la="exa -abghl --git --color=automatic"
  alias lan="la -snew"
end
