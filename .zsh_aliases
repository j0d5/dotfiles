####################################################
##                                                ##
## Johannes' alias settings                       ##
## date: 2010/03/22                               ##
##                                                ##
####################################################

if [ -f ~/.dotfiles/.global_aliases ]; then
	. ~/.dotfiles/.global_aliases
else
  echo ".global_aliases  not found!"
fi

alias -s de=firefox
alias -s org=firefox
alias -s com=firefox
alias -s net=firefox
alias -s c=gvim
alias -s h=gvim

